import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sudoku_block',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
